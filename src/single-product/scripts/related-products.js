/**
 * Related Products Script
 *
 * Fade-up animations are handled by initFadeUp (fp-fade-up.js) via [data-aos].
 * Color-dot switcher is handled by initColorDotSwitcher (home-popular-products.product-tabs.js).
 * Both are initialized in view.js with the .wp-block-etheme-single-product-index scope.
 *
 * This module is kept as a no-op export for forward compatibility.
 */

export function initRelatedProducts() {}
