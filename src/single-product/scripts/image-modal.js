/**
 * Image Modal Script
 *
 * This component's functionality has been merged into gallery.js
 * This file exists to maintain 1:1 mapping with components directory.
 */

export function initImageModal() {
	// Image modal functionality is now handled by gallery.js
	// This function exists for consistency but doesn't need to do anything
	// as the gallery script handles all modal interactions
}