/**
 * Order Received block frontend entry.
 */

import { initFadeUp } from '../../front-page/scripts/fp-fade-up.js';

document.addEventListener( 'DOMContentLoaded', function () {
	initFadeUp( '.order-received-block' );
} );
