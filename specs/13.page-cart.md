# Prompt optimizado para Claude Sonnet — Page Cart Header + Coupon Toggle

Actúa como un **senior WordPress/WooCommerce engineer** dentro del tema `bakka`, con foco en arquitectura modular por componentes PHP + JS progresivo.

## Objetivo

Implementar mejoras en la página de carrito (`page-cart`) para:

1. Replicar en cart el **header/sub-banner de posteos/shop** con el mismo look&feel de `/posteos` (sin breadcrumbs).
2. Rediseñar el bloque de coupon code con un patrón tipo MercadoLibre:
   - Estado inicial: trigger visual con `SVG + texto`.
   - Al click: se despliega el input/form de cupón.
3. Aplicar/reutilizar la lógica de animación **fade-up** en bloques relevantes de cart.
4. Mantener compatibilidad total con la lógica AJAX existente (no romper quantity, remove, coupon, totals, etc.).

## Contexto del proyecto (usar como fuente de verdad)

- `README.md`
- `src/page-cart/index/render.php`
- `templates/page-cart.html`
- `src/page-trabajos-realizados/index/render.php` (referencia del sub-banner)
- `src/core/components/sub-banner.php` (componente reusable)
- `src/front-page/scripts/fp-fade-up.js` (lógica fade-up reutilizable)
- `assets/icons/coupon.svg` (usar este asset, no crear otro)

## Requisitos funcionales detallados

### A) Header cart estilo posteos/shop (sin breadcrumbs)

- En `src/page-cart/index/render.php`:
  - Integrar `etheme_render_sub_banner()` de `src/core/components/sub-banner.php`.
  - Reutilizar exactamente el mismo lenguaje visual de `/posteos` (`sub_banner_con`).
  - **No renderizar breadcrumbs**.
  - Definir copy de título/subtítulo con intención comercial (incentivar compra).
  - Mantener estructura del bloque/orquestador de cart y compatibilidad con WooCommerce.

### B) Coupon code: trigger SVG + texto + toggle de input (estilo MercadoLibre)

- En `src/page-cart/components/coupon-form.php`:
  - Reemplazar la UI inicial del cupón por un trigger accesible (button) que incluya:
    - Ícono de `assets/icons/coupon.svg`.
    - Texto claro tipo “Agregar cupón”.
  - Al click, abrir/cerrar el form del coupon code (toggle).
  - Mantener funcionamiento actual de aplicar/remover cupones y mensajes.
  - No romper clases/selectores que usen scripts existentes; si cambias markup, actualizar JS de forma segura.
  - UX mobile-first similar a MercadoLibre:
    - trigger compacto y claro;
    - input + botón “Apply” con jerarquía visual limpia;
    - comportamiento responsive sin saltos de layout.

### C) Fade-up reutilizable en cart

- Reutilizar `initFadeUp()` desde `src/front-page/scripts/fp-fade-up.js` o extender enfoque equivalente sin duplicar lógica.
- Aplicar `data-aos="fade-up"` (y delays donde aporte) en bloques clave de cart:
  - sub-banner/content;
  - listado de items;
  - summary sidebar;
  - coupon block;
  - totales / CTA.
- Inicializar la animación scoped al wrapper de cart para evitar side effects globales.

### D) No regresiones

- Debe seguir funcionando:
  - update quantity;
  - remove item;
  - apply/remove coupon;
  - update totals;
  - empty cart state.
- Mantener i18n (`__`, `esc_html_e`, etc.), escaping y buenas prácticas WP.
- No agregar dependencias nuevas innecesarias.

## Restricciones técnicas

- Seguir arquitectura del tema:
  - `render.php` como orquestador.
  - Componentes en `src/page-cart/components/`.
  - JS modular en `scripts/` e inicialización en `view.js` cuando aplique.
- Evitar romper contratos existentes de data-attributes, IDs y hooks JS/AJAX.
- Reusar imágenes/assets ya presentes en el tema (no duplicar archivos).
- Mantener código modular, reusable y de responsabilidad única.

## Archivos objetivo (mínimo esperado)

- `src/page-cart/index/render.php`
- `src/page-cart/components/coupon-form.php`
- `src/page-cart/index/view.js` (si falta inicialización fade-up o toggle)
- `src/page-cart/scripts/coupon.js` (si requiere adaptar listeners al nuevo markup)
- `src/page-cart/index/style.scss` y/o estilos del grupo `src/page-cart/styles/*` (si existe la convención en este bloque)
- Cualquier archivo estrictamente necesario para registrar/enqueue si hiciera falta

## Criterios de aceptación

1. Cart muestra sub-banner visualmente consistente con `/posteos` y sin breadcrumbs.
2. El cupón inicia como trigger con `coupon.svg` + texto; al click abre/cierra input.
3. UX mobile/desktop coherente tipo e-commerce moderno (similar enfoque MercadoLibre).
4. Fade-up activo en secciones principales del cart usando lógica reutilizada.
5. No hay errores JS/PHP y no se rompe ninguna funcionalidad AJAX del carrito.
6. Código final respeta arquitectura modular del tema.

## Entregables esperados en tu respuesta

1. Resumen corto de qué cambiaste y por qué.
2. Lista de archivos modificados.
3. Notas de compatibilidad (qué se preservó para no romper AJAX).
4. Checklist de verificación manual (pasos concretos para probar cart).

Implementa los cambios directamente en el código del tema con enfoque conservador (sin regresiones), priorizando reuso de componentes core y consistencia visual con `/posteos`.
