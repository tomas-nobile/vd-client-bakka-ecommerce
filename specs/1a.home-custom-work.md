Eres un coding agent experto en WordPress Block Theme (FSE), PHP SSR, SCSS modular y JS vanilla.

Objetivo:
Replicar el diseño de la sección de referencia de Contrive (index1.html líneas 879-920, bloque `choose2-con`) en el tema WordPress `bakka`, creando una nueva sección del home llamada **custom work**.

Contexto obligatorio a respetar:
- Proyecto: `\\wsl.localhost\Ubuntu\home\tomas\clients-dk\wp-admin_ecommerce-test\wp-content\themes\bakka\`
- Arquitectura actual: orquestador en `src/front-page/index/render.php`, componentes en `src/front-page/components`, estilos en `src/front-page/styles`, inicialización JS en `src/front-page/index/view.js`.
- Template home: `templates/front-page.html` usa `etheme/front-page-index`.
- Referencia visual:
  - `0design-goal_envato_contrive/index1.html` (líneas 879-920)
  - `0design-goal_envato_contrive/assets/css`
  - `0design-goal_envato_contrive/assets/js`
- En caso de imágenes, usar las ya movidas al tema en `assets/images`.
- Imagen para esta sección: `assets/images/carpenter.png`.
- Reutilizar lógica de animación existente fade-up (no agregar librerías externas nuevas).

Requisitos funcionales y de contenido:
1) Crear componente nuevo para front-page llamado `custom-work` (PHP) y registrarlo dentro del flujo del home.
2) Insertar la sección **después de “Nuestros Productos Populares”** en `src/front-page/index/render.php`.
3) El texto debe comunicar muebles a medida (en español).
4) Botón con texto: **“Consultanos”**.
5) El botón debe llevar a WhatsApp usando la configuración global:
   - Archivo: `src/core/config/config.json`
   - Clave esperada: `social.whatsapp.url`
   - Si no existe URL válida, usar fallback seguro `#`.
6) Debe tener efecto parallax basado en scroll.
7) Mantener overlay oscuro para asegurar legibilidad del texto sobre la imagen.
8) Mantener estructura visual inspirada en referencia:
   - Columna izquierda: eyebrow, título, descripción, CTA
   - Columna derecha: tres bloques KPI (sin usar exactamente los mismos textos/números de la demo si no aportan al contexto)
9) No usar Bootstrap en la implementación final; puedes usar Tailwind utility classes cuando ayude al layout, y SCSS para el ajuste fino.
10) Prestar máxima atención a:
   - tipografías (familia, tamaño, grosor)
   - color y contraste
   - spacing/paddings/márgenes
   - ritmo vertical y jerarquía visual

Restricciones técnicas del tema:
- Mantener patrón modular del proyecto.
- No poner SCSS junto a componentes PHP.
- Crear estilos en `src/front-page/styles/*.scss` e importarlos desde `src/front-page/index/style.scss`.
- Escapar/sanitizar salida en PHP (`esc_html`, `esc_url`, `esc_attr`).
- No ejecutar `npm run build`.
- No modificar `build/`.

Implementación esperada (archivos):
1) `src/front-page/components/custom-work.php`
   - Render del bloque.
   - Fondo con `carpenter.png`.
   - Overlay oscuro.
   - `data-aos="fade-up"` donde corresponda.
   - CTA a WhatsApp leyendo config.

2) `src/front-page/styles/_custom-work.scss`
   - Estilos completos de la sección.
   - Replica visual fiel de referencia adaptada al sistema del tema.
   - Responsive mobile-first.
   - Estados hover/focus visibles en CTA.

3) `src/front-page/index/style.scss`
   - Importar `_custom-work.scss`.

4) `src/front-page/index/render.php`
   - Cargar componente.
   - Renderizar `custom-work` después de popular products.

5) JS parallax (reutilizable y limpio):
   - Opción A: nuevo archivo `src/front-page/scripts/home-custom-work-parallax.js`
   - Opción B: extender archivo de scripts existente equivalente.
   - Inicializar desde `src/front-page/index/view.js`.
   - Evitar listeners costosos; usar `requestAnimationFrame` o técnica eficiente.
   - Respetar `prefers-reduced-motion`.

6) Config social:
   - Usar `src/core/config/config.json` para WhatsApp (`social.whatsapp.url`).

Criterios de aceptación:
- La nueva sección aparece en home justo después de “Nuestros Productos Populares”.
- Visualmente coincide con la referencia (estructura, proporciones, tono, spacing, jerarquía).
- Usa `assets/images/carpenter.png`.
- Tiene fade-up consistente con otros componentes.
- Tiene parallax por scroll en frontend.
- CTA “Consultanos” abre WhatsApp.
- Estilos encapsulados y sin romper otras secciones.
- Código modular, legible y coherente con el proyecto.

Checklist de validación manual (entregar al final):
- [ ] Sección visible y orden correcto en home.
- [ ] Fondo + overlay correctos.
- [ ] Tipografía y spacing acordes a referencia.
- [ ] Animación fade-up funcionando.
- [ ] Parallax funcionando en desktop y comportamiento razonable en mobile.
- [ ] Botón “Consultanos” redirige a WhatsApp.
- [ ] Sin errores de lint en archivos editados.

Formato de respuesta esperado del agente al terminar:
1) Lista de archivos creados/editados.
2) Resumen breve de decisiones de implementación.
3) Riesgos o TODOs pendientes (si aplica).
