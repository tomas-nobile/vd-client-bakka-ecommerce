# Prompt para Claude Sonnet — Reutilizar cards de producto en Related Products

Actúa como **senior WordPress/WooCommerce theme engineer** y realiza una implementación real en este repo:

- Workspace: `\\wsl.localhost\Ubuntu\home\tomas\clients-dk\wp-admin_ecommerce-test\wp-content\themes\bakka`
- Contexto obligatorio a analizar antes de editar:
  - `README.md`
  - `src/single-product/index/render.php`
  - `templates/single-product.html`

## Objetivo principal

En `related products` del single product, reutilizar **la misma card de producto** que ya existe en Home y/o Archive, para que todas las cards queden **unificadas** visual y estructuralmente.

Referencia visual/DOM existente (debe respetarse):

1) Home Popular Products:
- Card root: `.pp-feature-box`
- Ejemplo en DOM: `section.popular-product ... > div.pp-feature-box[1]`
- Contenido esperado: badge de oferta + imagen + título + precio regular/tachado + precio actual

2) Grid de productos (archive):
- Variante clickable completa: `.pp-feature-box.pp-feature-box--full-link`
- Overlay link: `a.pp-feature-box__card-link` (card completa clickeable con `aria-label`)
- Ejemplo en DOM: `div.w-full ... > div.pp-feature-box.pp-feature-box--full-link > a.pp-feature-box__card-link`

## Requisitos funcionales

1. **Unificación real**
- No crear una “imitación” solo para single product.
- Reutilizar el mismo componente/markup/clases base de card para Home, Archive y Related Products.
- Si hoy hay duplicación entre Home y Archive, consolidar en un componente compartido dentro de `src/core/components/`.

2. **Sin reviews en single product**
- Quitar la sección/tab de reviews del single product.
- Comentar explícitamente en el código dónde estaba la lógica de reviews para dejar trazabilidad.

3. **Scope**
- Aplicar a **todas** las cards de Related Products.

4. **Card clickeable completa**
- Mantener patrón full-link (`a.pp-feature-box__card-link`) cuando corresponda.
- Incluir `aria-label` accesible con nombre del producto.

5. **Animación**
- Mantener/replicar animación `fade-up` usando el sistema existente del tema (`fp-ao` / clases y JS actuales).
- No inventar un sistema paralelo si ya existe uno reusable.

6. **Estilos en Core**
- Crear o mover estilos compartidos de card a `src/core/styles/*.scss`.
- Todas las cards (Home, Archive, Related) deben consumir ese estilo base común.
- Permitir **adaptación ligera responsive** (sin romper paridad visual).

7. **Assets**
- Si se necesitan imágenes/iconos, reutilizar las ya existentes en el tema (no descargar nuevas).

8. **Arquitectura del repo**
- Respetar patrón orquestador en `render.php`.
- Mantener componentes PHP reutilizables.
- Respetar convención del README: estilos complejos en `styles/*.scss` e importados desde los entry SCSS de bloque.

## Entregables esperados

1. Implementación de componente(s) compartido(s) en `src/core/components/` para card de producto.
2. Refactor de Home/Archive/Related para usar ese componente común.
3. Estilos compartidos en `src/core/styles/` e imports necesarios en cada bloque consumidor.
4. Related Products del single product renderizando exactamente la card unificada.
5. Fade-up funcionando en Related Products con el sistema existente.
6. Accesibilidad mínima validada (`aria-label`, estructura clickeable, escapes seguros en PHP).

## Restricciones de implementación

- No romper funcionalidad de WooCommerce (precios, sale badge, enlaces, stock si aplica).
- Mantener sanitización/escape (`esc_html`, `esc_attr`, `esc_url`, `wp_kses_post` según corresponda).
- Evitar lógica duplicada: si encuentras helpers equivalentes, centralízalos.
- Evitar cambios no relacionados.

## Plan de trabajo (ejecútalo)

1. Auditar dónde se renderiza hoy la card en Home y Archive.
2. Definir API mínima del componente core (args necesarios: producto, clases, full-link, animación, variantes).
3. Implementar componente compartido en `src/core/components/`.
4. Migrar render de cards de Home y Archive a ese componente.
5. Migrar `related-products` de `src/single-product/components/` para consumir el mismo componente.
6. Consolidar SCSS en `src/core/styles/` y actualizar imports en los bloques.
7. Verificar responsive con adaptación ligera.
8. Verificar que `fade-up` aplica en cards de related.
9. Reportar diff final por archivo + checklist de validación.

## Criterios de aceptación

- [ ] Related Products usa exactamente la misma estructura base de card que Home/Archive.
- [ ] Existe componente core reutilizable (no triplicación de markup).
- [ ] Existe SCSS compartido en `src/core/styles/` usado por todas las cards.
- [ ] Card completa clickeable con `a.pp-feature-box__card-link` + `aria-label`.
- [ ] Fade-up activo en related products.
- [ ] No se agregan assets nuevos innecesarios.
- [ ] Home y Archive no presentan regresión visual/funcional.

## Formato de salida requerido de tu ejecución

Responde con:

1. **Resumen breve** (qué se unificó y por qué).
2. **Archivos modificados** con propósito de cada cambio.
3. **Decisiones clave** (componente core, API, estrategia SCSS, fade-up).
4. **Checklist de aceptación** marcado.
5. **Riesgos o pendientes** (si los hubiera).
