# 24. Auditoría de seguridad del tema — Junio 2026

Informe de vulnerabilidades detectadas en una revisión completa del código del tema (PHP + JS en `src/`, `functions.php`). No se encontraron vulnerabilidades críticas ni altas: la postura general del tema es sólida. Se detectaron **2 hallazgos de severidad baja-media** y **3 observaciones de bajo riesgo**.

## Alcance y metodología

Se revisaron todas las superficies de entrada y salida:

- Los 14 handlers AJAX registrados (`wp_ajax_*` / `wp_ajax_nopriv_*`)
- Todos los archivos que leen `$_GET` / `$_POST` / `$_REQUEST` / `$_SERVER` (23 archivos)
- Todo `echo` de variables sin escape visible en el punto de salida (se verificó la asignación de cada una)
- Consultas SQL directas (`$wpdb`)
- Sinks de DOM XSS en JS (`innerHTML`, `insertAdjacentHTML`)
- Endpoints públicos firmados (recovery / unsubscribe de leads), export CSV, importador de Instagram
- Control de acceso en order-received, mi cuenta y páginas admin

---

## Hallazgos

### H1 — Newsletter: endpoint sin rate limiting ni confirmación de email

**Severidad: Baja-Media** · `src/front-page/includes/home-newsletter.ajax-handlers.php`

`etheme_ajax_newsletter_subscribe` es `nopriv` y solo exige el nonce (disponible para cualquier visitante de la home). A diferencia del endpoint de captura de leads — que sí tiene límite de 10 requests / 10 min por IP — este endpoint no tiene ningún límite, y tampoco envía email de confirmación (double opt-in).

**Impacto:**
1. Un at